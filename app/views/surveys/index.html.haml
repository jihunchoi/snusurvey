%h1 Listing surveys

%table
  %tr
    %th Title
    %th Description
    %th
    %th
    %th

  - @surveys.each do |survey|
    %tr
      %td= survey.title
      %td= survey.description
      %td= link_to 'Show', survey
      %td= link_to 'Edit', edit_survey_path(survey) if survey.editable?
      %td= link_to 'Destroy', survey, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Survey', new_survey_path
